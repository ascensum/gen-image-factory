Generate ONLY the SettingsPanel.tsx React component file. Do not create any other files, folders, or project structure.

Create a single TypeScript React component file named SettingsPanel.tsx with the following requirements:

COMPONENT: SettingsPanel.tsx
LOCATION: src/renderer/components/Settings/SettingsPanel.tsx

TECH STACK:
- React 18+ with TypeScript
- Tailwind CSS for styling
- Lucide React for icons

REQUIREMENTS:
- Main settings container with tab navigation
- Four main sections: API Keys, Files, Parameters, Advanced
- Auto-save functionality with debounced form updates
- Loading states and error handling
- Keyboard shortcuts (Ctrl+S to save)
- Responsive design for desktop window sizes

COMPONENT CODE STRUCTURE:
```typescript
import React, { useState, useEffect, useCallback } from 'react';
import { Eye, EyeOff, Save, RotateCcw, AlertCircle, CheckCircle } from 'lucide-react';

interface SettingsPanelProps {
  onSave?: (settings: any) => void;
  onReset?: () => void;
  isLoading?: boolean;
  error?: string | null;
  success?: string | null;
}

export const SettingsPanel: React.FC<SettingsPanelProps> = ({
  onSave,
  onReset,
  isLoading = false,
  error = null,
  success = null
}) => {
  // Component implementation here
};
```

FEATURES TO IMPLEMENT:
- Tab navigation between sections (API Keys, Files, Parameters, Advanced)
- Auto-save with 300ms debounce
- Loading indicators for async operations
- Error message display
- Success confirmation messages
- Keyboard shortcuts support (Ctrl+S to save, Ctrl+Z to reset)
- Window size adaptation

STYLING REQUIREMENTS:
- Use Tailwind CSS classes only
- Responsive design for different window sizes
- Smooth transitions and hover effects
- Proper focus management
- Accessible color contrast

ACCESSIBILITY REQUIREMENTS:
- Proper ARIA labels and roles
- Keyboard navigation support
- Screen reader friendly
- Focus indicators
- Error announcements

Generate ONLY the complete SettingsPanel.tsx file with all imports, interfaces, and implementation. Do not create any other files or project structure. 