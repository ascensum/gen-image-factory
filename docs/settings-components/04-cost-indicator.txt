Generate ONLY the CostIndicator.tsx React component file. Do not create any other files, folders, or project structure.

Create a single TypeScript React component file named CostIndicator.tsx with the following requirements:

TECH STACK:
- React 18+ with TypeScript
- Tailwind CSS for styling
- Lucide React for icons

COMPONENT: CostIndicator.tsx
LOCATION: src/renderer/components/Settings/CostIndicator.tsx

REQUIREMENTS:
- Cost display badges with color-coded levels
- Estimated cost calculations for API features
- Tooltips with detailed pricing information
- Different size variants (small, medium, large)
- Integration with paid features

FEATURES:
- Color-coded cost levels (free, low, medium, high)
- Estimated cost calculations
- Detailed tooltips with pricing info
- Multiple size variants
- Icon indicators for cost levels
- Hover effects with detailed information

COMPONENT STRUCTURE:
<CostIndicator>
  <div className="inline-flex items-center space-x-1">
    <span className={`inline-flex items-center px-2 py-1 rounded-full text-xs font-medium ${getCostColor(costLevel)}`}>
      <CostIcon className="w-3 h-3 mr-1" />
      {getCostLabel(costLevel)}
    </span>
    <button
      type="button"
      onClick={showTooltip}
      className="text-gray-400 hover:text-gray-600"
      aria-label="Show cost details"
    >
      <InfoIcon className="w-4 h-4" />
    </button>
  </div>
  <Tooltip>
    <div className="p-2 text-sm">
      <h4 className="font-medium">Cost Details</h4>
      <p className="text-gray-600">{costDescription}</p>
      <p className="text-xs text-gray-500 mt-1">Estimated: {estimatedCost}</p>
    </div>
  </Tooltip>
</CostIndicator>

PROPS INTERFACE:
interface CostIndicatorProps {
  costLevel: 'free' | 'low' | 'medium' | 'high' | 'unknown';
  estimatedCost?: string;
  costDescription?: string;
  size?: 'small' | 'medium' | 'large';
  showTooltip?: boolean;
  className?: string;
}

COST LEVELS:
- Free: Green badge, no cost
- Low: Blue badge, minimal cost
- Medium: Yellow badge, moderate cost
- High: Red badge, significant cost
- Unknown: Gray badge, cost unknown

SIZE VARIANTS:
- Small: text-xs, compact layout
- Medium: text-sm, standard layout
- Large: text-base, prominent layout

COLOR SCHEME:
- Free: bg-green-100 text-green-800
- Low: bg-blue-100 text-blue-800
- Medium: bg-yellow-100 text-yellow-800
- High: bg-red-100 text-red-800
- Unknown: bg-gray-100 text-gray-800

FEATURES:
- Color-coded badges based on cost level
- Estimated cost display
- Detailed tooltips with pricing info
- Icon indicators for visual clarity
- Hover effects for additional information
- Accessible design with proper ARIA labels

STYLING:
- Use Tailwind CSS classes
- Consistent color scheme
- Proper contrast ratios
- Smooth transitions
- Responsive design

ACCESSIBILITY:
- Proper ARIA labels and descriptions
- Screen reader friendly
- Keyboard navigation support
- High contrast color combinations
- Clear visual indicators

TOOLTIP FEATURES:
- Detailed cost breakdown
- Pricing information
- Usage estimates
- Feature descriptions
- Links to pricing pages

DO NOT INCLUDE:
- External tooltip libraries
- Complex animations
- External pricing APIs
- External UI libraries

Generate ONLY the complete CostIndicator.tsx file with all imports, interfaces, and implementation. Do not create any other files or project structure. 